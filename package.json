{
  "name": "trivial-redux-authorization",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p lib && coffee -o ./lib -c ./src",
    "pub": "npm run build && npm publish"
  },
  "author": "",
  "license": "ISC"
}
